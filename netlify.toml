# Purpose: Netlify build settings for Remix + Vite.
[build]
  command = "npm run build"
  publish = "build/client"

[build.environment]
  # Ensure devDependencies are installed for Remix/Vite builds.
  NPM_CONFIG_PRODUCTION = "false"

[dev]
  command = "npm run dev"
